{
  "buildCommand": "cd time-tracker && npm install && npm run build",
  "outputDirectory": "time-tracker/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    }
  ],
  "functions": {
    "api/index.js": {
      "runtime": "nodejs20.x"
    }
  }
}
